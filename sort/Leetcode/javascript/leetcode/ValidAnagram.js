const s = "anagram"
const t = "nagaram"

function isAnagram(s, t) {


    
};

/*
let newPerson = "David"
if(stringMap.hasOwnProperty(newPerson)) {
    let currentCount = stringMap[newPerson];
    stringMap[newPerson] = currentCount + 1;
} else {
    stringMap[newPerson] = 1;
}

if(stringMap.hasOwnProperty(newPerson)) {
    let currentCount = stringMap[newPerson];
    stringMap[newPerson] = currentCount + 1;
} else {
    stringMap[newPerson] = 1;
}
console.log(stringMap);
*/